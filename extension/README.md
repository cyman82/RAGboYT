# YouTube RAG Extension

## Setup

1. Install dependencies:

   npm install

2. Start dev build:

   npm run dev

3. Load the extension from the `build` folder in Chrome.

## Environment

Set `PLASMO_PUBLIC_API_BASE_URL` to your FastAPI server, e.g.:

PLASMO_PUBLIC_API_BASE_URL=http://localhost:8000/api
